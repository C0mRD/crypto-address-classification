from setuptools import setup, find_packages

setup(
    name='pipeline',  # Package name
    version='0.1',  # Package version
    packages=find_packages('pipeline'),  # Automatically find all packages
)